# Names only. Set values on Cloudflare Pages — never commit them.

# Server-only. Absolute URL the /go function redirects to.
MEGAPOT_PLAY_DESTINATION=

# Server-only. Reserved for a future on-chain template. Unused by this shell.
MEGAPOT_REFERRER_ADDRESS=

# Server-only. Data API key if you add server reads later.
MEGAPOT_API_KEY=
